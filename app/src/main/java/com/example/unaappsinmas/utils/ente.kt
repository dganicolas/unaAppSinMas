package com.example.unaappsinmas.utils

import androidx.compose.runtime.mutableStateOf

object ente{
    var indexHamburgesa = mutableStateOf(0)  // Usamos mutableStateOf para que sea mutable
    var indexDonwBar = mutableStateOf(0)
}

